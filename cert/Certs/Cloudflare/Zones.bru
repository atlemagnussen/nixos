meta {
  name: Zones
  type: http
  seq: 4
}

get {
  url: {{baseApiUrl}}/zones?name=logout.work
  body: none
  auth: bearer
}

params:query {
  name: logout.work
}

auth:bearer {
  token: {{API_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
