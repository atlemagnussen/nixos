meta {
  name: Token verify
  type: http
  seq: 3
}

get {
  url: {{baseApiUrl}}/user/tokens/verify
  body: none
  auth: bearer
}

auth:bearer {
  token: {{API_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
