meta {
  name: DNS records
  type: http
  seq: 1
}

get {
  url: {{baseApiUrl}}/zones/{{ZONE_ID}}/dns_records?type=TXT
  body: none
  auth: bearer
}

params:query {
  type: TXT
}

auth:bearer {
  token: {{API_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
